# mock | real — default mock runs without backend or API keys
NEXT_PUBLIC_API_MODE=mock
NEXT_PUBLIC_API_URL=http://localhost:8080

# Go API (make dev)
PORT=8080
# DATABASE_URL=postgres://areeza:areeza@localhost:5432/areeza?sslmode=disable

# Optional sidecars (see docs/dev-setup.md §3b) — unset = in-proc keyword router for /api/classify
# CLASSIFIER_API_URL=http://localhost:8081
# RAG_API_URL=http://localhost:8082
