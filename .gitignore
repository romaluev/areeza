# dependencies
node_modules/
.pnpm-store/

# builds / framework
.next/
.turbo/
dist/
out/
build/
*.tsbuildinfo

# env & secrets (commit .env.example only)
.env
.env.local
.env*.local

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# local agent config (each dev keeps their own)
.claude/

# scratch & tooling (not part of the product)
.deck/
.prototype/

# render artifacts
*.pdf
slide-*.jpg
slide-*.png
